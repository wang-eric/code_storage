import pandas as pd
from openpyxl import load_workbook
from openpyxl.styles import Font
import argparse

def analyze_unique_values(data_file, columns_file, output_file, sheet_name=None):
    """
    Analyze unique values in specified columns of an Excel file.
    
    Args:
        data_file (str): Path to the Excel file containing data
        columns_file (str): Path to the Excel file containing column names to analyze
        output_file (str): Path to save the results
        sheet_name (str, optional): Specific sheet name to analyze in data_file
    """
    
    try:
        # Read the column names to analyze
        columns_df = pd.read_excel(columns_file)
        columns_to_analyze = columns_df.iloc[:, 0].tolist()  # Assuming columns are in first column
        
        # Read the data file
        if sheet_name:
            df = pd.read_excel(data_file, sheet_name=sheet_name)
        else:
            df = pd.read_excel(data_file)
        
        # Prepare results
        results = []
        
        # Analyze each specified column
        for column in columns_to_analyze:
            if column in df.columns:
                unique_count = df[column].nunique()
                results.append({
                    'Column Name': column,
                    'Unique Values Count': unique_count,
                    'Sample Unique Values': ', '.join(map(str, df[column].dropna().unique()[:5]))  # Show first 5 unique values
                })
            else:
                results.append({
                    'Column Name': column,
                    'Unique Values Count': 'Column not found',
                    'Sample Unique Values': 'N/A'
                })
        
        # Convert results to DataFrame
        results_df = pd.DataFrame(results)
        
        # Save to Excel with formatting
        with pd.ExcelWriter(output_file, engine='openpyxl') as writer:
            results_df.to_excel(writer, index=False, sheet_name='Unique Value Analysis')
            
            # Access the workbook and worksheet for formatting
            workbook = writer.book
            worksheet = writer.sheets['Unique Value Analysis']
            
            # Apply formatting
            header_font = Font(bold=True)
            for cell in worksheet[1]:  # First row (header)
                cell.font = header_font
            
            # Auto-adjust column widths
            for column in worksheet.columns:
                max_length = 0
                column_letter = column[0].column_letter
                for cell in column:
                    try:
                        if len(str(cell.value)) > max_length:
                            max_length = len(str(cell.value))
                    except:
                        pass
                adjusted_width = (max_length + 2) * 1.2
                worksheet.column_dimensions[column_letter].width = adjusted_width
        
        print(f"Analysis complete. Results saved to {output_file}")
        
    except Exception as e:
        print(f"An error occurred: {str(e)}")

if __name__ == "__main__":
    # Set up command line arguments
    parser = argparse.ArgumentParser(description='Analyze unique values in specified columns of an Excel file.')
    parser.add_argument('data_file', help='Path to the Excel file containing data')
    parser.add_argument('columns_file', help='Path to the Excel file containing column names to analyze')
    parser.add_argument('output_file', help='Path to save the results')
    parser.add_argument('--sheet', help='Specific sheet name to analyze in data_file', default=None)
    
    args = parser.parse_args()
    
    # Run the analysis
    analyze_unique_values(
        data_file=args.data_file,
        columns_file=args.columns_file,
        output_file=args.output_file,
        sheet_name=args.sheet
    )
